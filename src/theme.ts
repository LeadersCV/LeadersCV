// src/theme.ts

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#002463",
    },
    secondary: {
      main: "#FCB730",
    },
  },
});

export default theme;
